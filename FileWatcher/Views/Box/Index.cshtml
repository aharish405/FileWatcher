@using NonFactors.Mvc.Grid
<div class="card">
    <div class="card-header d-flex justify-content-between align-items-center">
        <span>Boxes</span>
        <a class="btn btn-success btn-sm" asp-action="Create">+ Create New Box</a>
    </div>
    <div class="card-body">
        @Html.AjaxGrid(Url.Action("Grid", "Box"))
    </div>
</div>
